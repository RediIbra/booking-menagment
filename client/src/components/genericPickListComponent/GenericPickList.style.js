import styled from "styled-components";
import { FormControl } from "@mui/material";

export const StyledPickList = styled(FormControl)`
  width: ${(props) => props.width};
  height: ${(props) => props.height};
`;
